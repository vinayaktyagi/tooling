// import React from "react";

// import App from "./App";
describe("App", () => {
  it("sanity test", () => {
    let a = true;
    expect(a).toBe(false);
  });

  it("renders App without error", () => {
    // render(<App />);
  });
});
